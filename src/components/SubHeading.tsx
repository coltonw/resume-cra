import React from 'react';

const SubHeading = ({ children }: { children: React.ReactNode }) => (
  <div className="mt-8 mb-4 block justify-between lg:flex">{children}</div>
);

export default SubHeading;
