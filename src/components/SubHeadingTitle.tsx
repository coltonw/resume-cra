import React from 'react';

const SubHeadingTitle = ({ children }: { children: React.ReactNode }) => (
  <h2 className="text-l font-black text-stone-800">{children}</h2>
);

export default SubHeadingTitle;
